# macOS
.DS_Store
**/.DS_Store

# Archivos de trabajo / inventarios locales
*.csv
# …salvo el respaldo de las direcciones de los QR: si se pierde todo, es lo
# único que permite que las etiquetas ya pegadas en la pared vuelvan a servir.
!qr-direcciones.csv
*.xlsx
*.xls

# Fotografías: viven en Supabase Storage, no en el repositorio.
# La carpeta reducida se genera y se sube desde subir-fotos.html
imagenes pabellon 414/
fotos-web/

# Dependencias / entorno (por si se agregan más adelante)
node_modules/
.env
.env.local
.vercel
